class Logger {
    // req: string;
  
    // title: string | undefined;
  
    // columns: Column[];
  
    constructor({
    //   id = uuidv4(),
    //   title = 'title',
    //   columns = [new Column()]
    } = {}) {
    //   this.id = id;
    //   this.title = title;
    //   this.columns = columns.map((column) => new Column(column));
    }
}